<%= simple_form_for @fraction do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%#= f.input :parliament_member_id, collection: (@fraction.order('name')), label: "Nama Wakil Rakyat"%>
    <%#= f.association :parliament_member, label_method: :fraksi_gabung_nama, label: "Nama Wakil Rakyat" %>
    <%#= f.association :level, as: :radio_buttons, label: "Pangkat / Jabatan" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
